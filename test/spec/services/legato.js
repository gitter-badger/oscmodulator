'use strict';

describe('Service: legato', function () {

  // load the service's module
  beforeEach(module('oscmodulatorApp'));

  // instantiate service
  beforeEach(inject(function () {
  }));

  it('should do something', function () {
    //expect(!!legato).toBe(true);
  });

});
