'use strict';

describe('Service: midi', function () {

  // load the service's module
  beforeEach(module('oscmodulatorApp'));

  // instantiate service
  /*
  var midi;
  beforeEach(inject(function (_midi_) {
    midi = _midi_;
  }));

  it('should do something', function () {
    expect(!!midi).toBe(true);
  });
  */

});
